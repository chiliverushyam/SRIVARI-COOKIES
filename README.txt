SRIVARI COOKIES — CUSTOMER READY BUILD

Important:
1. This build fixes the mobile footer overflow and adds mandatory-field labels with red *.
2. Product pack sizes are now 200g and 400g.
3. Exact 200g/400g prices and exact delivery charge were NOT supplied, so the build intentionally does not invent them. Add the real values in app.js/config.js before accepting payments.
4. Until prices and delivery charge are configured, Add to Cart/checkout will not accept an unpriced pack.
5. Payment remains "awaiting_payment" until a real Razorpay verification/webhook is connected.
6. Use the supplied clean HD images in assets/.
